{
    "name": "email-distribution",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc",
        "start": "node dist/server.js",
        "start:dev": "ts-node-dev src/server.ts",
        "run-once": "ts-node-dev --transpile-only src/run-once.ts"
    },
    "dependencies": {
        "axios": "^1.6.0",
        "amqplib": "^0.10.0",
        "node-cron": "^3.0.0"
    },
    "devDependencies": {
        "@types/amqplib": "^0.10.0",
        "@types/node-cron": "^3.0.0",
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0",
        "ts-node-dev": "^2.0.0"
    }
}